integrations:
  jenkins:
    - host: localhost:8080
      username: ${JENKINS_USERNAME}
      password: ${JENKINS_TOKEN}
  ### Example for how to add your Github Actions instance using the API:
  # - host: github.com
  #   username: guest
  #   password: guest
